using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Turret_Spot : MonoBehaviour
{
    public bool mounted = false;    // Used to detect whether a turret spot is already occupied or not

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
